// ltl-passwd-gen/main.ltl
//
// Cryptographically secure password and passphrase generator.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-passwd-gen v0.1.0")
    io::println("Cryptographically secure password and passphrase generator.")
    0
}
