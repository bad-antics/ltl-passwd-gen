// examples/demo.ltl — runnable demo for ltl_passwd_gen

import std::io
use ltl_passwd_gen as lib

fn main() {
    io::println("ltl_passwd_gen demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}